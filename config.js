var config = [];
config.push({
	steam_user: "..",
	steam_pass: "..",
	sharedSecret: "..",
	identity_secret: ".."
});
config.push({
	steam_user: "..",
	steam_pass: "..",
	sharedSecret: "..",
	identity_secret: ".."
});
module.exports = config;